/*
comparision operatore are used to compare two values or objects
ex. ==, <, >, <= >= these will comapre two values are will try to convert these values to anpther data type if the data types of these values are different
but to check the values strictely means data types also we need to compare we will use ===
below are some another examples
console.log(null > 0); // false
console.log(null == 0); // flase
console.log(null >= 0); // true
// comparision works differently in JS it treats ==, === differently but <, >, <=, >= treats differently
console.log(undefined == 0);  // false
console.log(undefined > 0);// false
console.log(undefined < 0);// false
*/
console.log(typeof Object) //function
console.log(typeof Array) //function
console.log(typeof null) //object
console.log(typeof undefined)//undefined
console.log(typeof Function)//function
