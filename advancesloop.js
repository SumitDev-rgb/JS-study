// ********************* forof loop *****************

let arr = [1,2,3,4,5]

for (const index of arr) { // here what is happening is from arr each value is coming from array saving in index
    console.log(index);
    
}