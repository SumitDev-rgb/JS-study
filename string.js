
let name = 'sumit';
console.log(name)

let f = new String("Mohit")

// console.log(name.indexOf("s")); // this will return the index place of character M
// console.log(name.charAt(0)); // this will return the character of given index 
// console.log(name.concat(f)) // this will concal both string
// == and === ; this will compare both string 1. based on the CSSMathValue, 2. based on value and the data type means will campare strictely
// console.log(name.endsWith("t")) // this will show if the goven string ends with given parameter return true or flase
// console.log(name.startsWith("t")) // this will return true or flase to check if given string starts with passed value
// console.log(name.slice(1,3)) // um // this will slice the given string index to end value but does consider the end value also we can give nagative values
// console.log("     thrre   ".trim()); // thrre // this method will remove the startig space and end space
// it has also some other methods like trimStart() // will remove the space from start and trimEnd() // this will remove the space from end
// it work only on white space 
// includes() // this will check for the value in the given string and will return trueor flase
// split(' ') // this will covert the string into array based on given parameter in method