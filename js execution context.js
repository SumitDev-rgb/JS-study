// whenever we want to run any file JS will run the file in two context(JS is a single threaded language)
// 1. global execution context (will run happen always and will allocation this phase to "this" keyword) 2. FUnctional Execution Context(happen only for the functions)

// there are two phases where the globalcontext run the file
// 1. Menory Phase 2. Execution Phase